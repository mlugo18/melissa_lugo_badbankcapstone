version 1 of bad bank
uses starter files as the baseline

Steps:
- Update express node and install mongo db and install cors
    * npm install express
    * npm install cors
    * npm install mongodb
	